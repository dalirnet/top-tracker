#### Sample

```js
import { {{class}} } from 'top-tracker'

const parameters = {}
const input = {}

new {{class}}(parameters)
    .call(input)
    .then((output) => {})
    .catch(({ message, payload }) => {})
```
